let minimist,fetch,help,configure,version;_328‍.x([["cli",()=>cli]]);_328‍.w("minimist",[["default",["minimist"],function(v){minimist=v}]]);_328‍.w("./fetch",[["fetch",["fetch"],function(v){fetch=v}]]);_328‍.w("./help",[["help",["help"],function(v){help=v}]]);_328‍.w("./configure",[["configure",["configure"],function(v){configure=v}]]);_328‍.w("./version",[["version",["version"],function(v){version=v}]]);





       async function cli(argsArray) {
  const args = minimist(argsArray.slice(2));
  let cmd = args._[0] || 'help';

  if (args.version || args.v) {
    cmd = 'version';
  }

  if (args.help || args.h) {
    cmd = 'help';
  }

  switch (cmd) {
    case 'version':
      version(args);
      break;

    case 'help':
      help(args);
      break;

    case 'fetch':
      fetch(args);
      break;

    case 'config':
      configure(args);
      break;

    default:
      console.error(`"${cmd}" is not a valid command!`);
      break;
  }
}
