let minimist,version;_454‍.x([["cli",()=>cli]]);_454‍.w("minimist",[["default",["minimist"],function(v){minimist=v}]]);_454‍.w("./version",[["version",["version"],function(v){version=v}]]);
// import { now } from './now';
// import { forecast } from './forecast';
// import { help } from './help';
// import { configure } from './configure';


       async function cli(argsArray) {
  const args = minimist(argsArray.slice(2));
  let cmd = args._[0] || 'help';

  if (args.version || args.v) {
    cmd = 'version';
  }

  if (args.help || args.h) {
    cmd = 'help';
  }

  switch (cmd) {
    case 'version':
      version(args);
      break;

    // case 'help':
    //   help(args);
    //   break;
    //
    // case 'now':
    //   now(args);
    //   break;
    //
    // case 'forecast':
    //   forecast(args);
    //   break;
    //
    // case 'config':
    //   configure(args);
    //   break;

    default:
      console.error(`"${cmd}" is not a valid command!`);
      break;
  }
}
